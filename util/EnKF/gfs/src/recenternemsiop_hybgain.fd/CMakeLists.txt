cmake_minimum_required(VERSION 2.6)
if(BUILD_UTIL)
  file(GLOB LOCAL_SRC ${CMAKE_CURRENT_SOURCE_DIR}/*.f90)
  set_source_files_properties( ${LOCAL_SRC} PROPERTIES COMPILE_FLAGS ${UTIL_Fortran_FLAGS} )
  add_executable(recenternemsiop_hybgain.x  ${LOCAL_SRC} )
  set_target_properties( recenternemsiop_hybgain.x PROPERTIES COMPILE_FLAGS ${UTIL_Fortran_FLAGS} )
  SET( CMAKE_EXE_LINKER_FLAGS  "${CMAKE_EXE_LINKER_FLAGS} ${OpenMP_Fortran_FLAGS}" )
  include_directories( ${sigio_inc4} ${nemsio_inc} ) 
  target_link_libraries( recenternemsiop_hybgain.x sigio_4 nemsio bacio_4 w3nco_4 ${MPI_Fortran_LIBRARIES} )
endif()
