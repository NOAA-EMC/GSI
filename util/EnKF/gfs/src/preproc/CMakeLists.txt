cmake_minimum_required(VERSION 2.6)
if(BUILD_UTIL)
  file(GLOB LOCAL_SRC ${CMAKE_CURRENT_SOURCE_DIR}/*.f90)
  set(Util_MODULE_DIRECTORY "${PROJECT_BINARY_DIR}/include/preproc")
  add_executable(preproc.x  ${LOCAL_SRC} )
  set_target_properties( preproc.x PROPERTIES COMPILE_FLAGS ${UTIL_Fortran_FLAGS} )
  set_target_properties( preproc.x PROPERTIES Fortran_MODULE_DIRECTORY ${Util_MODULE_DIRECTORY} )
  include_directories( ${SIGIOINC} ) 
  target_link_libraries( preproc.x ${SIGIO_LIBRARY} )
  
endif()
