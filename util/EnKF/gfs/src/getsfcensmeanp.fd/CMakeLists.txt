cmake_minimum_required(VERSION 2.6)
if(BUILD_UTIL)
  file(GLOB LOCAL_SRC ${CMAKE_CURRENT_SOURCE_DIR}/*.f90)
  set(Util_MODULE_DIRECTORY "${PROJECT_BINARY_DIR}/include/adderspec")
  set_source_files_properties( ${LOCAL_SRC} PROPERTIES COMPILE_FLAGS ${UTIL_Fortran_FLAGS} )
  add_executable( getsfcensmeanp.x  ${LOCAL_SRC} )
  set_target_properties(  getsfcensmeanp.x PROPERTIES COMPILE_FLAGS ${UTIL_Fortran_FLAGS} )
  set_target_properties(  getsfcensmeanp.x PROPERTIES COMPILE_FLAGS ${Util_MODULE_DIRECTORY} )
  SET( CMAKE_EXE_LINKER_FLAGS  "${CMAKE_EXE_LINKER_FLAGS} ${OMPFLAG}" )
  include_directories( ${CORE_INCS} ) 
  target_link_libraries(  getsfcensmeanp.x ${CORE_BUILT} ${CORE_LIBRARIES} ${CORE_BUILT} ${CORE_LIBRARIES} ${MPI_Fortran_LIBRARIES} )
endif()
