cmake_minimum_required(VERSION 2.6)
if(BUILD_UTIL)
  file(GLOB LOCAL_SRC ${CMAKE_CURRENT_SOURCE_DIR}/*.f90)
  set_source_files_properties( ${LOCAL_SRC} PROPERTIES COMPILE_FLAGS ${UTIL_Fortran_FLAGS} )
  add_executable(getsigensmeanp_smooth_ncep.x  ${LOCAL_SRC} )
  set_target_properties( getsigensmeanp_smooth_ncep.x PROPERTIES COMPILE_FLAGS ${UTIL_Fortran_FLAGS} )
  SET( CMAKE_EXE_LINKER_FLAGS  "${CMAKE_EXE_LINKER_FLAGS} ${OMPFLAG}" )
  include_directories( ${CORE_INCS} ) 
  target_link_libraries( getsigensmeanp_smooth_ncep.x ${CORE_LIBRARIES} ${MPI_Fortran_LIBRARIES} )
  add_dependencies( getsigensmeanp_smooth_ncep.x enkfdeplib enkflib )
endif()
