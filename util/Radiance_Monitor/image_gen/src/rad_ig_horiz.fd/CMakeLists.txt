cmake_minimum_required(VERSION 2.6)
  file(GLOB RAD_IG_HORIZ_SRC ${CMAKE_CURRENT_SOURCE_DIR}/*90 )
  set(RAD_IG_HORIZ_Fortran_FLAGS "-fp-model strict -assume byterecl -convert big_endian -O3 -traceback -D_REAL8_ ")
  set(Util_MODULE_DIR ${PROJECT_BINARY_DIR}/include/rad_ig_horiz.x )
  set_source_files_properties( ${RAD_IG_HORIZ_SRC} PROPERTIES COMPILE_FLAGS ${RAD_IG_HORIZ_Fortran_FLAGS} )
  add_executable(rad_ig_horiz.x  ${RAD_IG_HORIZ_SRC} )
  set_target_properties( rad_ig_horiz.x PROPERTIES COMPILE_FLAGS ${RAD_IG_HORIZ_Fortran_FLAGS} )
  set_target_properties( rad_ig_horiz.x PROPERTIES Fortran_MODULE_DIRECTORY ${Util_MODULE_DIR} )
  include_directories( ${CORE_INCS} ) 
  target_link_libraries( rad_ig_horiz.x -L./ ${W3NCO_4_LIBRARY} )
  if(BUILD_W3NCO)
    add_dependencies( rad_ig_horiz.x ${W3NCO_4_LIBRARY} )
  endif()

