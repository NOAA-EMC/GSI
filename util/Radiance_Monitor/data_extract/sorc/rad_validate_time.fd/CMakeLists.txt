cmake_minimum_required(VERSION 2.6)
  file(GLOB RAD_VALIDATE_TIME_SRC ${CMAKE_CURRENT_SOURCE_DIR}/*90 )
  set(RAD_VALIDATE_TIME_Fortran_FLAGS "-fp-model strict -assume byterecl -convert big_endian -O3 -traceback -D_REAL8_ ")
  set(Util_MODULE_DIR ${PROJECT_BINARY_DIR}/include/rad_validate_time.x )
  set_source_files_properties( ${RAD_VALIDATE_TIME_SRC} PROPERTIES COMPILE_FLAGS ${RAD_VALIDATE_TIME_Fortran_FLAGS} )
  add_executable(rad_validate_time.x  ${RAD_VALIDATE_TIME_SRC} )
  set_target_properties( rad_validate_time.x PROPERTIES COMPILE_FLAGS ${RAD_VALIDATE_TIME_Fortran_FLAGS} )
  set_target_properties( rad_validate_time.x PROPERTIES Fortran_MODULE_DIRECTORY ${Util_MODULE_DIR} )
  include_directories( ${CORE_INCS} ) 
  target_link_libraries( rad_validate_time.x -L./ ${W3NCO_4_LIBRARY} )
  if(BUILD_W3NCO)
    add_dependencies( rad_validate_time.x ${W3NCO_4_LIBRARY} )
  endif()

