Usage: (under csh)

  set NYMD=20000101; set NHMS=000000
  echo "&setup nymd=$NYMD nhms=$NHMS \
       satinfo_tmpl='satinfo.tmpl' satinfo_outf='satinfo.txt' \
       dbname='./sidb' /" | ./mksi.x

where NAMELIST /setup/ contains (see setup.nml for an example),

nymd:		yyyymmdd, e.g. 20000101 for Jan. 1, 2000.
nhms:		hhmmss, e.g. 000000 for 00z
satinfo_tmpl:	file of the input satinfo template ("satinfo.tmpl")
satinfo_outf:	file of the output satinfo data ("satinfo.txt")
dbname:		directory containing satinfo database tables ("."):
satops_tbl:	file of active operation periods ("satops.tbl")
satinst_tbl:	file of all channels ("satinst.tbl")
actvchan_tbl:	file of active channel periods ("active_channels.tbl")
usblchan_tbl:	file of usable channel periods ("usable_channels.tbl")


Variables defined in main.F90 and/or mksi.fig:

[..]	-- run time variables
{..}	-- external ASCII files.
*	-- a modified data set.

_nm	-- satellite names, such as, n14, n15, aqua, etc.
_sn	-- sensor names, such as hirs2, msu, ssu, etc.
_ch	-- channel numbers, 2, 3, 4, etc.
_si	-- tag of bundled sensor/instrument/satellite names.
_iu	-- an inclusion flag of 1 (in-use) or -1 (not-in-use).

[ymd:hms]	-- a date-time tag defined as in (/20000101,120000/)
{satops}	-- a stem-leaves table of active satellites
[asat_*]	-- data of active satellites at [ymd:hms]
{active_channels} -- a stem-leaves table of active channels
[actv_*]	-- data of active channels at [ymd:hms]
{usable_channels} -- a stem-leaves table of usable (passive) channels
[usbl_*]	-- data of usable channels at [ymd:hms]
{satinst}	-- a stem-leaves table of all channels
[sens_*]	-- data of channels of active satellites
{satinfo_tmpl}	-- a template of satinfo
[tmpl_*]	-- data of satinfo
{satinfo_txt}	-- a generated satinfo at [ymd:hms]


Build:

1. In gsi/, do "./configure", to create a Makefile.conf for Makefile in
  this directory, mksi/.
2. In mksi/, do "make all" to build the excutable ./mksi.x, or do
  "make test" to build and to make test of ./mksi.x.
